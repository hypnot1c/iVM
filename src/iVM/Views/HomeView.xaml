<Page
    x:Class="iVM.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iVM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <SplitView DisplayMode="CompactOverlay" CompactPaneLength="50" OpenPaneLength="150" IsPaneOpen="True">
    <SplitView.Pane>
      <StackPanel>
        <Button>Click me</Button>
        <TextBlock Text="Icon" />
      </StackPanel>
    </SplitView.Pane>
    <SplitView.Content>
      <StackPanel Orientation="Vertical" Margin="20">
        <TextBox x:Name="MyMessage" />
        <TextBlock x:Name="Title" Text="11111111111111" />
        <Button x:Name="SayHello" Content="Say hello" Margin="0,10" />
        </StackPanel>
    </SplitView.Content>
  </SplitView>
</Page>
